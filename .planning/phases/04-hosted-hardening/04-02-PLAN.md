---
phase: 04-hosted-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - src/app/page.tsx
  - src/app/channels/page.tsx
  - src/app/channel/[channel]/page.tsx
  - src/app/video/[videoId]/page.tsx
  - src/lib/catalog.ts
  - src/lib/catalog-db.ts
  - src/lib/insights.ts
  - src/lib/runtime-stream.ts
  - src/lib/runtime-batches.ts
  - src/app/api/video/route.ts
  - src/app/api/insight/route.ts
  - src/app/api/insight/stream/route.ts
  - src/lib/__tests__/catalog-cache.test.ts
  - src/lib/__tests__/catalog-repository.test.ts
  - src/lib/__tests__/insight-stream-route.test.ts
  - scripts/benchmark-hosted-scale.ts
  - docs/architecture/system-overview.md
  - docs/operations/hosted-scale-validation.md
  - README.md
  - .planning/phases/04-hosted-hardening/04-SCALE-REPORT.md
autonomous: true
requirements:
  - PERF-02
user_setup: []
must_haves:
  truths:
    - The repo contains one repeatable validation path for checking browse and runtime behavior near the 1000-video target instead of relying on informal judgment.
    - Phase 4 measures and, where necessary, improves the actual page-generation and runtime-read seams that shape hosted behavior instead of publishing a benchmark against side paths only.
    - Scale-readiness work preserves the current SQLite plus filesystem architecture unless evidence shows the next threshold has been crossed.
    - The phase ends with an explicit documented answer for when the current stack remains good enough and what conditions would trigger the next storage/runtime step beyond SQLite.
    - Hosted scale validation covers both browse reads and runtime observability paths, including status or SSE behavior that matter during active analysis.
    - Operator documentation remains aligned with the app’s private, friend-group deployment model rather than drifting into generic SaaS scaling advice.
  artifacts:
    - src/app/page.tsx
    - src/app/channels/page.tsx
    - src/app/channel/[channel]/page.tsx
    - src/app/video/[videoId]/page.tsx
    - scripts/benchmark-hosted-scale.ts
    - docs/operations/hosted-scale-validation.md
    - .planning/phases/04-hosted-hardening/04-SCALE-REPORT.md
    - src/lib/__tests__/catalog-cache.test.ts
    - src/lib/__tests__/catalog-repository.test.ts
  key_links:
    - The benchmark or validation script measures the same SQLite and filesystem-backed code paths that production browse pages, video pages, and runtime routes use.
    - The final threshold document feeds the roadmap’s future `SCALE-01` decision instead of forcing a premature migration in Phase 4.
    - Browse-page query strategy and runtime read amplification are treated as first-class scale seams, not as follow-up ideas outside the measured path.
---

<objective>
Validate hosted scale behavior near the 1000-video target and document the next-step threshold beyond SQLite plus filesystem storage.

Purpose: Phase 4 should finish with evidence, not vibes. This plan creates a repeatable validation path for the current hosted architecture and records the practical threshold that would justify the next architecture step only when real pressure appears.
Output: A benchmark or measurement path tied to real browse/runtime code, targeted regression coverage for hot paths, and a durable hosted scale report that documents acceptable behavior and escalation thresholds.
</objective>

<execution_context>
@/Users/ossieirondi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ossieirondi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-hosted-hardening/04-RESEARCH.md
@.planning/phases/04-hosted-hardening/04-VALIDATION.md
@.planning/phases/04-hosted-hardening/04-01-PLAN.md
@src/app/page.tsx
@src/app/channels/page.tsx
@src/app/channel/[channel]/page.tsx
@src/app/video/[videoId]/page.tsx
@src/lib/catalog.ts
@src/lib/catalog-db.ts
@src/lib/insights.ts
@src/lib/runtime-stream.ts
@src/lib/runtime-batches.ts
@src/app/api/video/route.ts
@src/app/api/insight/route.ts
@src/app/api/insight/stream/route.ts
@src/lib/__tests__/catalog-cache.test.ts
@src/lib/__tests__/catalog-repository.test.ts
@src/lib/__tests__/insight-stream-route.test.ts
@README.md
@docs/architecture/system-overview.md
@docs/plans/2026-03-09-self-hosted-proxmox-deployment.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a repeatable hosted-scale validation path against the actual browse pages and runtime reads</name>
  <files>scripts/benchmark-hosted-scale.ts, src/app/page.tsx, src/app/channels/page.tsx, src/app/channel/[channel]/page.tsx, src/app/video/[videoId]/page.tsx, src/lib/catalog.ts, src/lib/catalog-db.ts, src/lib/insights.ts, src/lib/runtime-stream.ts, src/lib/runtime-batches.ts, src/app/api/video/route.ts, src/app/api/insight/route.ts, src/app/api/insight/stream/route.ts</files>
  <action>Add a repeatable benchmark or measurement script that exercises the actual SQLite-backed browse queries, page-generation paths, and filesystem-backed insight/status reads the hosted app uses. Include the home page, channels page, channel detail page, video page, and runtime observability reads rather than limiting the work to sidecar APIs. Keep it compatible with the repo’s current private runtime model, and prefer fixture-backed or synthetic catalog generation that can approximate the 1000-video target without destructive data rewrites. Include enough output to capture request-shape timings, repeated-read behavior, browse hydration costs, and any concurrent SSE or status-read observations relevant to hosted analysis viewing.</action>
  <verify>node scripts/benchmark-hosted-scale.ts --check</verify>
  <done>The repo can now rerun one concrete hosted-scale validation flow against the same browse pages and runtime reads that determine real hosted behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Reduce browse/runtime read amplification where evidence shows practical pressure</name>
  <files>src/app/page.tsx, src/app/channels/page.tsx, src/app/channel/[channel]/page.tsx, src/app/video/[videoId]/page.tsx, src/lib/catalog.ts, src/lib/catalog-db.ts, src/lib/insights.ts, src/lib/runtime-stream.ts, src/lib/runtime-batches.ts, src/app/api/video/route.ts, src/app/api/insight/route.ts, src/app/api/insight/stream/route.ts, src/lib/__tests__/catalog-cache.test.ts, src/lib/__tests__/catalog-repository.test.ts, src/lib/__tests__/insight-stream-route.test.ts, src/lib/__tests__/runtime-stream.test.ts, src/lib/__tests__/runtime-batches.test.ts</files>
  <action>Use the benchmark findings to tighten the actual browse/runtime seams that matter for the 1000-video question. Favor selective SQLite query helpers or narrower page data loads over full-catalog hydration where practical, and keep runtime observability paths honest about repeated filesystem or stream work under concurrent viewers. Keep the phase additive: improve or expose measurement points where needed, but do not replace SQLite or move provider/runtime responsibilities into the UI. Add or extend tests so the scale report is backed by automated coverage of the key browse and runtime-read assumptions.</action>
  <verify>npx vitest run src/lib/__tests__/catalog-cache.test.ts src/lib/__tests__/catalog-repository.test.ts src/lib/__tests__/insight-stream-route.test.ts src/lib/__tests__/runtime-stream.test.ts src/lib/__tests__/runtime-batches.test.ts</verify>
  <done>Scale validation is backed by concrete improvements or explicit confirmations in the key browse and runtime-read paths rather than a one-off script alone.</done>
</task>

<task type="auto">
  <name>Task 3: Publish the hosted scale report and the next-step threshold beyond SQLite</name>
  <files>docs/operations/hosted-scale-validation.md, docs/architecture/system-overview.md, README.md, .planning/phases/04-hosted-hardening/04-SCALE-REPORT.md</files>
  <action>Document how to run the hosted-scale validation path, what near-1000-video behavior was observed, and the explicit threshold for when the current SQLite plus filesystem model stops being the right near-term answer. Make the report concrete about triggers such as catalog size, write concurrency, operator pain, or SSE/runtime pressure, and tie that threshold to the already-declared future `SCALE-01` direction instead of prematurely expanding scope in this phase.</action>
  <verify>rg -n "1000|SQLite|filesystem|threshold|SCALE-01|hosted-scale" README.md docs/architecture/system-overview.md docs/operations/hosted-scale-validation.md .planning/phases/04-hosted-hardening/04-SCALE-REPORT.md && npx prettier --check README.md docs/architecture/system-overview.md docs/operations/hosted-scale-validation.md .planning/phases/04-hosted-hardening/04-SCALE-REPORT.md</verify>
  <done>The phase ends with a durable operator-facing answer about whether the current stack is sufficient and what exact signals should trigger the next architecture step.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node scripts/benchmark-hosted-scale.ts --check`
- [ ] `npx vitest run src/lib/__tests__/catalog-cache.test.ts src/lib/__tests__/catalog-repository.test.ts src/lib/__tests__/insight-stream-route.test.ts src/lib/__tests__/runtime-stream.test.ts src/lib/__tests__/runtime-batches.test.ts`
- [ ] `npx eslint scripts/benchmark-hosted-scale.ts src/app/page.tsx src/app/channels/page.tsx src/app/channel/[channel]/page.tsx src/app/video/[videoId]/page.tsx src/lib/catalog.ts src/lib/catalog-db.ts src/lib/insights.ts src/lib/runtime-stream.ts src/lib/runtime-batches.ts src/app/api/video/route.ts src/app/api/insight/route.ts src/app/api/insight/stream/route.ts src/lib/__tests__/catalog-cache.test.ts src/lib/__tests__/catalog-repository.test.ts src/lib/__tests__/insight-stream-route.test.ts src/lib/__tests__/runtime-stream.test.ts src/lib/__tests__/runtime-batches.test.ts`
- [ ] `npx prettier --check README.md docs/architecture/system-overview.md docs/operations/hosted-scale-validation.md .planning/phases/04-hosted-hardening/04-SCALE-REPORT.md`
- [ ] `rg -n "1000|SQLite|filesystem|threshold|SCALE-01|benchmark" README.md docs .planning/phases/04-hosted-hardening`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo provides a repeatable hosted-scale validation path near the 1000-video target
- Browse-page and runtime hot paths have targeted automated coverage supporting the scale-readiness conclusion
- The phase documents a concrete next-step threshold beyond SQLite plus filesystem storage without prematurely migrating away from the current private architecture
  </success_criteria>

<output>
After completion, create `.planning/phases/04-hosted-hardening/04-02-SUMMARY.md`
</output>
