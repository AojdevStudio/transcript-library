---
phase: 04-hosted-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/instrumentation.ts
  - src/app/layout.tsx
  - src/lib/hosted-config.ts
  - src/lib/private-api-guard.ts
  - src/app/api/raw/route.ts
  - src/app/api/analyze/route.ts
  - src/app/api/analyze/status/route.ts
  - src/app/api/video/route.ts
  - src/app/api/channel/route.ts
  - src/app/api/channels/route.ts
  - src/app/api/insight/route.ts
  - src/app/api/insight/stream/route.ts
  - src/app/api/sync-hook/route.ts
  - src/lib/catalog.ts
  - src/lib/catalog-db.ts
  - src/lib/analysis.ts
  - src/lib/insights.ts
  - src/lib/__tests__/hosted-config.test.ts
  - src/lib/__tests__/private-api-guard.test.ts
  - src/lib/__tests__/route-access-control.test.ts
  - src/lib/__tests__/sync-hook-route.test.ts
  - README.md
  - docs/architecture/system-overview.md
autonomous: true
requirements:
  - DEPLOY-03
  - SAFE-01
user_setup: []
must_haves:
  truths:
    - Hosted deployments fail early with actionable operator guidance from a concrete server bootstrap/preflight entrypoint before hosted traffic is served.
    - The app encodes one explicit private-boundary policy for internal API routes instead of relying on implicit perimeter trust alone.
    - Transcript, artifact, and runtime-trigger endpoints reject untrusted callers while preserving the current private deployment model and keeping app-level SaaS auth out of scope.
    - Sensitive route payloads stop leaking internal filesystem paths, artifact path bundles, raw provider/runtime details, or other internals that are unnecessary for trusted private clients.
    - Existing private automation behavior for `SYNC_TOKEN` remains compatible, but it is aligned with the broader hosted guard model instead of staying special-cased forever.
    - Local development remains low-friction and does not require hosted-only configuration just to browse or iterate locally.
  artifacts:
    - src/instrumentation.ts
    - src/lib/hosted-config.ts
    - src/lib/private-api-guard.ts
    - src/lib/insights.ts
    - src/lib/__tests__/hosted-config.test.ts
    - src/lib/__tests__/private-api-guard.test.ts
    - src/lib/__tests__/route-access-control.test.ts
    - README.md
  key_links:
    - Hosted startup validation and route enforcement share the same runtime assumptions so deploy docs, tests, and route behavior do not drift apart.
    - The private-boundary helper is reused across read, stream, and mutation routes instead of duplicating auth logic in handlers.
    - Route guarding and payload redaction move together so Phase 4 does not “protect” endpoints while still returning hosted-sensitive internals.
---

<objective>
Harden the private hosted runtime by adding explicit startup/deploy validation and a shared private API boundary for sensitive routes.

Purpose: Phase 4 starts by turning the current hosted assumptions into enforceable application behavior. This plan keeps the app private and compatibility-preserving while making bad deploys fail early and internal APIs stop depending on ambient trust alone.
Output: One hosted preflight validation layer, one reusable private-boundary helper for sensitive APIs, route coverage proving denial/allow behavior, and operator docs that match the implemented hosted contract.
</objective>

<execution_context>
@/Users/ossieirondi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ossieirondi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-hosted-hardening/04-RESEARCH.md
@.planning/phases/04-hosted-hardening/04-VALIDATION.md
@.planning/phases/03-durable-runtime/03-01-PLAN.md
@.planning/phases/03-durable-runtime/03-04-PLAN.md
@CLAUDE.md
@AGENTS.md
@src/instrumentation.ts
@src/app/layout.tsx
@src/app/api/raw/route.ts
@src/app/api/analyze/route.ts
@src/app/api/analyze/status/route.ts
@src/app/api/video/route.ts
@src/app/api/channel/route.ts
@src/app/api/channels/route.ts
@src/app/api/insight/route.ts
@src/app/api/insight/stream/route.ts
@src/app/api/sync-hook/route.ts
@src/lib/catalog.ts
@src/lib/catalog-db.ts
@src/lib/analysis.ts
@README.md
@docs/architecture/system-overview.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add one hosted runtime preflight validator and wire it to a concrete bootstrap entrypoint</name>
  <files>src/instrumentation.ts, src/app/layout.tsx, src/lib/hosted-config.ts, src/lib/catalog.ts, src/lib/catalog-db.ts, src/lib/analysis.ts, src/lib/__tests__/hosted-config.test.ts</files>
  <action>Create a shared hosted-runtime validation helper that distinguishes permissive local development from strict hosted mode. Validate required env vars and runtime paths for the private hosted deployment contract, including transcript repo availability, configured insights/catalog roots, writability/readability assumptions, and any required CLI/runtime dependencies. Wire this validator to a concrete server bootstrap entrypoint such as `src/instrumentation.ts` so hosted mode fails before the app serves trusted traffic; do not leave the fail-fast contract as lazy first-request validation. Preserve the existing low-friction local defaults and avoid destructive directory rewrites.</action>
  <verify>npx vitest run src/lib/__tests__/hosted-config.test.ts</verify>
  <done>Hosted misconfiguration is surfaced by one reusable validator from a concrete bootstrap path before operators discover it via broken browsing or analysis actions.</done>
</task>

<task type="auto">
  <name>Task 2: Introduce one private-boundary helper and apply it across sensitive internal routes</name>
  <files>src/lib/private-api-guard.ts, src/app/api/raw/route.ts, src/app/api/analyze/route.ts, src/app/api/analyze/status/route.ts, src/app/api/video/route.ts, src/app/api/channel/route.ts, src/app/api/channels/route.ts, src/app/api/insight/route.ts, src/app/api/insight/stream/route.ts, src/lib/__tests__/private-api-guard.test.ts</files>
  <action>Implement one shared request guard that expresses the app-side private deployment boundary for transcript, artifact, and runtime-sensitive APIs. Support the chosen trusted-caller signals for the hosted model, such as reverse-proxy or Cloudflare Access headers and an operator-controlled bearer token path where appropriate, but keep app-level multi-user auth out of scope. Apply the helper consistently across sensitive read, stream, and mutation routes so policy lives in one place rather than drifting route by route. Include route-level tests that prove the actual handlers deny untrusted requests and allow trusted ones.</action>
  <verify>npx vitest run src/lib/__tests__/private-api-guard.test.ts src/lib/__tests__/route-access-control.test.ts</verify>
  <done>Sensitive internal APIs now share one explicit trust policy and can prove route-level allow/deny behavior through focused tests.</done>
</task>

<task type="auto">
  <name>Task 3: Align sync-hook compatibility, redact sensitive payloads, and update hosted operator docs</name>
  <files>src/app/api/raw/route.ts, src/app/api/video/route.ts, src/app/api/insight/route.ts, src/app/api/insight/stream/route.ts, src/app/api/sync-hook/route.ts, src/lib/private-api-guard.ts, src/lib/insights.ts, src/lib/__tests__/sync-hook-route.test.ts, src/lib/__tests__/private-api-guard.test.ts, src/lib/__tests__/route-access-control.test.ts, README.md, docs/architecture/system-overview.md</files>
  <action>Preserve the existing `SYNC_TOKEN` automation flow while aligning it with the new shared private-boundary model, including clear responses for denied access and misconfigured hosted state. Explicitly redact or reshape sensitive payload fields on transcript/artifact/runtime routes so private clients no longer receive unnecessary absolute paths, artifact path bundles, or raw provider/runtime details that are irrelevant to the hosted contract. Update operator-facing docs to explain hosted mode prerequisites, trusted-caller expectations, payload-sensitivity rules, and which routes are intentionally private in this friend-group deployment. Keep the docs concise and explicit about what is not being added in this phase, especially SaaS-style auth or UI-level provider switching.</action>
  <verify>npx vitest run src/lib/__tests__/sync-hook-route.test.ts src/lib/__tests__/private-api-guard.test.ts src/lib/__tests__/route-access-control.test.ts src/lib/__tests__/hosted-config.test.ts && npx prettier --check README.md docs/architecture/system-overview.md</verify>
  <done>Hosted route policy, payload redaction, sync-hook compatibility, and deployment docs all describe the same private boundary and fail-fast behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/lib/__tests__/hosted-config.test.ts src/lib/__tests__/private-api-guard.test.ts src/lib/__tests__/route-access-control.test.ts src/lib/__tests__/sync-hook-route.test.ts`
- [ ] `npx eslint src/instrumentation.ts src/app/layout.tsx src/lib/hosted-config.ts src/lib/private-api-guard.ts src/app/api/raw/route.ts src/app/api/analyze/route.ts src/app/api/analyze/status/route.ts src/app/api/video/route.ts src/app/api/channel/route.ts src/app/api/channels/route.ts src/app/api/insight/route.ts src/app/api/insight/stream/route.ts src/app/api/sync-hook/route.ts src/lib/catalog.ts src/lib/catalog-db.ts src/lib/analysis.ts src/lib/insights.ts src/lib/__tests__/hosted-config.test.ts src/lib/__tests__/private-api-guard.test.ts src/lib/__tests__/route-access-control.test.ts src/lib/__tests__/sync-hook-route.test.ts`
- [ ] `npx prettier --check README.md docs/architecture/system-overview.md`
- [ ] `rg -n "hosted|private|SYNC_TOKEN|Access|trusted|guard|redact|sensitive" src lib src/app/api README.md docs/architecture/system-overview.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Hosted deployments fail early from a concrete bootstrap/preflight entrypoint when critical runtime prerequisites are missing
- Sensitive internal APIs explicitly enforce the private hosted trust boundary instead of relying only on ambient perimeter assumptions
- Sensitive route payloads no longer expose unnecessary hosted internals after access control succeeds
- Existing private automation remains compatible through the shared guard model
- Local development remains compatible and low-friction
  </success_criteria>

<output>
After completion, create `.planning/phases/04-hosted-hardening/04-01-SUMMARY.md`
</output>
