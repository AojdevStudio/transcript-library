---
phase: 01-artifact-foundations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - src/lib/analysis.ts
  - src/modules/analysis/index.ts
  - src/lib/insights.ts
  - src/lib/headless-youtube-analysis.ts
  - scripts/nightly-insights.ts
  - src/lib/__tests__/insights-base-dir.test.ts
  - src/lib/__tests__/insight-paths.test.ts
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - SAFE-03
  - TEST-01
user_setup: []
must_haves:
  truths:
    - Runtime insight paths resolve from INSIGHTS_BASE_DIR when configured and fall back to repo data/insights when unset or blank.
    - All primary insight read and write helpers use shared path resolution rather than rebuilding artifact roots ad hoc.
    - Invalid or unsafe videoId values are rejected at filesystem helper boundaries used by this phase.
  artifacts:
    - vitest.config.ts
    - src/lib/__tests__/insights-base-dir.test.ts
    - src/lib/__tests__/insight-paths.test.ts
    - src/lib/analysis.ts
  key_links:
    - insightsBaseDir() is used by analysis, insights, and metadata-path helpers.
    - The new server-side test harness is runnable from package scripts and covers configured and default artifact roots.
---

<objective>
Establish the shared artifact-path foundation for Phase 1 by adding a lightweight test harness, implementing configurable base-dir resolution, and routing all core insight path helpers through it.

Purpose: This is the deployment-unblocking base layer for the rest of the phase. Without a single path authority, the hosted Proxmox artifact directory will drift across modules and the later structured-analysis work will inherit that inconsistency.
Output: A working `INSIGHTS_BASE_DIR` resolver, updated path helpers, and automated tests covering default/configured path behavior.
</objective>

<execution_context>
@/Users/ossieirondi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ossieirondi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-artifact-foundations/01-CONTEXT.md
@.planning/phases/01-artifact-foundations/01-RESEARCH.md
@docs/plans/2026-03-09-configurable-insights-base-dir.md
@src/lib/analysis.ts
@src/lib/insights.ts
@src/lib/headless-youtube-analysis.ts
@src/modules/analysis/index.ts
@src/modules/insights/index.ts
@scripts/nightly-insights.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and wire the Phase 1 server-side test harness</name>
  <files>package.json, vitest.config.ts, src/lib/__tests__/insights-base-dir.test.ts, src/lib/__tests__/insight-paths.test.ts</files>
  <action>Add Vitest as the first lightweight server-side test runner for this repo. Create `vitest.config.ts`, add the minimal package script support, and author failing tests for `insightsBaseDir()` and cross-module insight-path resolution. Keep the harness node-focused and small; do not introduce broader test infrastructure than Phase 1 needs.</action>
  <verify>npx vitest run src/lib/__tests__/insights-base-dir.test.ts src/lib/__tests__/insight-paths.test.ts</verify>
  <done>Vitest runs from the repo, the new tests exist, and they fail only for missing path-resolution behavior rather than missing test infrastructure.</done>
</task>

<task type="auto">
  <name>Task 2: Implement shared base-dir and safe artifact path resolution</name>
  <files>src/lib/analysis.ts, src/modules/analysis/index.ts, src/lib/insights.ts, src/lib/headless-youtube-analysis.ts, scripts/nightly-insights.ts</files>
  <action>Implement `insightsBaseDir()` so it honors non-blank `INSIGHTS_BASE_DIR`, resolves absolute paths, and falls back to `process.cwd()/data/insights`. Replace duplicated artifact-root construction in analysis, insight, metadata, and artifact-producing batch workflows with shared helpers. Include `scripts/nightly-insights.ts` if it still hardcodes the legacy artifact root. Tighten any Phase 1-touched path helpers so unsafe `videoId` values cannot silently construct filesystem paths.</action>
  <verify>npx vitest run src/lib/__tests__/insights-base-dir.test.ts src/lib/__tests__/insight-paths.test.ts</verify>
  <done>All primary Phase 1 artifact helpers resolve under the configured base dir, local fallback still works, and the tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Document and regression-check the deploy-path contract</name>
  <files>package.json, vitest.config.ts, src/lib/analysis.ts, src/lib/insights.ts, src/lib/headless-youtube-analysis.ts, scripts/nightly-insights.ts, src/lib/__tests__/insights-base-dir.test.ts, src/lib/__tests__/insight-paths.test.ts</files>
  <action>Finish the plan by making sure the test names and code comments communicate the production-path contract clearly, especially that Phase 1 blesses `INSIGHTS_BASE_DIR=/srv/transcript-library/insights` for hosted deployments while preserving local defaults. Keep comments concise and only where the behavior is not obvious from the code.</action>
  <verify>npx eslint src/lib/analysis.ts src/lib/insights.ts src/lib/headless-youtube-analysis.ts src/modules/analysis/index.ts src/modules/insights/index.ts scripts/nightly-insights.ts src/lib/__tests__/insights-base-dir.test.ts src/lib/__tests__/insight-paths.test.ts && npx prettier --check package.json vitest.config.ts && npx vitest run src/lib/__tests__/insights-base-dir.test.ts src/lib/__tests__/insight-paths.test.ts</verify>
  <done>The codebase has one clear artifact-root authority, the tests are stable, and the deploy-path behavior is obvious to future maintainers.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/lib/__tests__/insights-base-dir.test.ts src/lib/__tests__/insight-paths.test.ts`
- [ ] `npx eslint src/lib/analysis.ts src/lib/insights.ts src/lib/headless-youtube-analysis.ts src/modules/analysis/index.ts src/modules/insights/index.ts scripts/nightly-insights.ts src/lib/__tests__/insights-base-dir.test.ts src/lib/__tests__/insight-paths.test.ts`
- [ ] `npx prettier --check package.json vitest.config.ts`
- [ ] `rg -n 'process\\.cwd\\(\\).*data.*insights|path\\.join\\(process\\.cwd\\(\\), \"data\", \"insights\"\\)' src/lib src/modules scripts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Configured and default insight roots both work through shared helpers
- Phase 1 path helpers are safer and easier for later plans to build on
  </success_criteria>

<output>
After completion, create `.planning/phases/01-artifact-foundations/01-01-SUMMARY.md`
</output>
