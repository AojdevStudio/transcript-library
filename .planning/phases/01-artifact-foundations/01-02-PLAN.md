---
phase: 01-artifact-foundations
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/lib/analysis-contract.ts
  - src/lib/headless-youtube-analysis.ts
  - src/lib/analysis.ts
  - src/lib/curation.ts
  - src/lib/insights.ts
  - src/modules/curation/index.ts
  - src/app/api/insight/route.ts
  - src/components/VideoAnalysisWorkspace.tsx
  - .claude/skills/HeadlessYouTubeAnalysis/SKILL.md
  - src/lib/__tests__/analysis-contract.test.ts
  - src/lib/__tests__/headless-analysis-prompt.test.ts
  - src/lib/__tests__/insight-legacy-fallback.test.ts
autonomous: true
requirements:
  - ANLY-01
  - ANLY-02
  - ANLY-03
  - ANLY-04
  - TEST-02
user_setup: []
must_haves:
  truths:
    - New analysis runs validate provider output against the strict structured contract before structured fields reach the UI.
    - Successful runs write both analysis.json and analysis.md from the validated payload.
    - Existing markdown-only insights remain readable through an explicit transition-safe fallback until the one-time migration is run.
    - Invalid structured output causes explicit failed status and visible error state rather than silently degrading into partial UI content.
  artifacts:
    - src/lib/analysis-contract.ts
    - src/lib/__tests__/analysis-contract.test.ts
    - src/lib/__tests__/headless-analysis-prompt.test.ts
    - src/lib/__tests__/insight-legacy-fallback.test.ts
    - .claude/skills/HeadlessYouTubeAnalysis/SKILL.md
  key_links:
    - The prompt contract, parser, runtime writer, and UI reader all agree on the same field names.
    - Legacy markdown-only insight reads remain available while the migration script has not yet been run.
    - Contract failures propagate into both persisted run/error state and user-visible UI state.
---

<objective>
Adopt the strict structured-analysis contract end to end: prompt for it, parse it, persist it, and expose it to the current UI without redesigning the interface.

Purpose: Phase 1 needs deterministic structured fields instead of markdown heuristics for summary/takeaway/action-item rendering. This plan turns the user’s locked contract into the authoritative runtime path.
Output: A validated `analysis.json` flow, derived `analysis.md`, and UI/runtime behavior that surfaces contract failures clearly.
</objective>

<execution_context>
@/Users/ossieirondi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ossieirondi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-artifact-foundations/01-CONTEXT.md
@.planning/phases/01-artifact-foundations/01-RESEARCH.md
@.planning/phases/01-artifact-foundations/01-01-SUMMARY.md
@docs/plans/2026-03-09-structured-analysis-json-contract.md
@src/lib/analysis.ts
@src/lib/headless-youtube-analysis.ts
@src/lib/curation.ts
@src/components/VideoAnalysisWorkspace.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define and test the strict structured analysis parser</name>
  <files>src/lib/analysis-contract.ts, src/modules/curation/index.ts, src/lib/__tests__/analysis-contract.test.ts</files>
  <action>Create the structured analysis parser/type boundary in its own library module. Cover valid payloads, fenced JSON, missing required fields, invalid array fields, and empty `reportMarkdown`. Favor explicit validation and readable failure messages over silent cleanup. Keep the contract aligned with the currently approved skill-defined field set.</action>
  <verify>npx vitest run src/lib/__tests__/analysis-contract.test.ts</verify>
  <done>The repo has a strict parser module with passing tests for success and failure cases, and downstream code has a stable import path for the structured contract.</done>
</task>

<task type="auto">
  <name>Task 2: Update the worker prompt and runtime write path for JSON-first analysis</name>
  <files>src/lib/headless-youtube-analysis.ts, .claude/skills/HeadlessYouTubeAnalysis/SKILL.md, src/lib/analysis.ts, src/lib/__tests__/headless-analysis-prompt.test.ts</files>
  <action>Align the headless analysis prompt and any supporting skill instructions with the already-approved strict JSON contract, without redesigning that contract during execution. Then update the runtime so successful provider output is parsed, validated, written to `analysis.json`, and converted into `analysis.md`. Make failure handling explicit so bad JSON becomes a failed run with usable error information in logs and metadata.</action>
  <verify>npx vitest run src/lib/__tests__/analysis-contract.test.ts src/lib/__tests__/headless-analysis-prompt.test.ts</verify>
  <done>The prompt contract is explicit, successful runs persist both canonical artifacts, and invalid structured payloads move runs into a clear failed state.</done>
</task>

<task type="auto">
  <name>Task 3: Swap UI-facing structured sections to validated data and preserve report rendering</name>
  <files>src/lib/curation.ts, src/lib/insights.ts, src/app/api/insight/route.ts, src/components/VideoAnalysisWorkspace.tsx, src/lib/__tests__/insight-legacy-fallback.test.ts</files>
  <action>Update the insight-reading path so summary, takeaways, action items, and related sections come from validated structured data first. Preserve full markdown report rendering through `analysis.md`, and ensure the existing UI surfaces contract failures in a visible failed/error state without redesigning the component. Critically, keep markdown-only legacy insights readable through an explicit transition-safe fallback until Plan 03’s one-time migration is run, and prove that behavior with an automated test rather than broad smoke coverage alone.</action>
  <verify>npx vitest run src/lib/__tests__/analysis-contract.test.ts src/lib/__tests__/headless-analysis-prompt.test.ts src/lib/__tests__/insight-legacy-fallback.test.ts && npm run e2e</verify>
  <done>The UI uses validated structured sections, full report rendering still works, legacy markdown-only insights still render during the migration window, and failure states are visible in both logs and UI state.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/lib/__tests__/analysis-contract.test.ts src/lib/__tests__/headless-analysis-prompt.test.ts src/lib/__tests__/insight-legacy-fallback.test.ts`
- [ ] `npx eslint src/lib/analysis-contract.ts src/lib/headless-youtube-analysis.ts src/lib/analysis.ts src/lib/curation.ts src/lib/insights.ts src/modules/curation/index.ts src/app/api/insight/route.ts src/components/VideoAnalysisWorkspace.tsx src/lib/__tests__/analysis-contract.test.ts src/lib/__tests__/headless-analysis-prompt.test.ts src/lib/__tests__/insight-legacy-fallback.test.ts`
- [ ] `npx prettier --check .claude/skills/HeadlessYouTubeAnalysis/SKILL.md`
- [ ] `npm run e2e`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Structured UI fields come from validated JSON rather than markdown heuristics
- Legacy markdown-only insights remain readable until the migration script is applied
- Invalid structured provider output is obvious to both operator and user
  </success_criteria>

<output>
After completion, create `.planning/phases/01-artifact-foundations/01-02-SUMMARY.md`
</output>
