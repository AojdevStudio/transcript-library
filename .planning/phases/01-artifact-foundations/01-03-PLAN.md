---
phase: 01-artifact-foundations
plan: 03
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - scripts/backfill-insight-artifacts.ts
  - scripts/migrate-legacy-insights-to-json.ts
  - src/lib/insights.ts
  - src/app/api/insight/route.ts
  - src/components/VideoAnalysisWorkspace.tsx
  - README.md
  - docs/architecture/artifact-schema.md
  - docs/architecture/analysis-runtime.md
  - docs/plans/2026-03-09-self-hosted-proxmox-deployment.md
  - src/lib/__tests__/legacy-artifact-migration.test.ts
autonomous: true
requirements:
  - DEPLOY-01
  - DEPLOY-02
  - ANLY-03
  - SAFE-03
user_setup: []
must_haves:
  truths:
    - Existing markdown-only insights can be upgraded through an explicit one-time migration path instead of requiring permanent long-lived legacy behavior.
    - Post-migration app behavior is JSON-first while preserving analysis.md as the human-readable report artifact.
    - Phase 1 docs describe the canonical hosted path and the operator expectations for artifact storage on Proxmox.
    - Operators have a concrete way to confirm the migration window is complete and that no legacy markdown-only artifacts remain to be upgraded.
  artifacts:
    - scripts/migrate-legacy-insights-to-json.ts
    - data/insights/.migration-status.json
    - src/lib/__tests__/legacy-artifact-migration.test.ts
    - docs/architecture/artifact-schema.md
    - docs/architecture/analysis-runtime.md
  key_links:
    - Migration tooling produces artifacts that the new insight-read path can consume without special-case UI rewrites.
    - Migration and backfill scripts reuse the same safe base-dir and `videoId` path helpers as runtime code.
    - Deployment docs, runtime code, and artifact schema all name the same production base-dir convention.
    - The migration workflow includes a machine-checkable completion signal or zero-legacy-artifact check.
---

<objective>
Complete Phase 1 by providing the one-time legacy migration path, narrowing compatibility behavior, and updating the docs and operator workflow for the new JSON-first artifact model.

Purpose: The previous plans make the new artifact contract possible; this plan makes it operationally real and prevents Phase 1 from shipping with indefinite compatibility debt.
Output: A migration script, tested upgrade behavior, JSON-first steady-state read path, and updated deployment/runtime documentation.
</objective>

<execution_context>
@/Users/ossieirondi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ossieirondi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-artifact-foundations/01-CONTEXT.md
@.planning/phases/01-artifact-foundations/01-RESEARCH.md
@.planning/phases/01-artifact-foundations/01-01-SUMMARY.md
@.planning/phases/01-artifact-foundations/01-02-SUMMARY.md
@docs/plans/2026-03-09-configurable-insights-base-dir.md
@docs/plans/2026-03-09-structured-analysis-json-contract.md
# Treat this deployment doc as stale input to be corrected in this plan where it still assumes markdown-first structured output.
@docs/plans/2026-03-09-self-hosted-proxmox-deployment.md
@scripts/backfill-insight-artifacts.ts
@src/lib/insights.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and test the one-time legacy insight migration path</name>
  <files>scripts/backfill-insight-artifacts.ts, scripts/migrate-legacy-insights-to-json.ts, src/lib/__tests__/legacy-artifact-migration.test.ts</files>
  <action>Create an explicit migration script for existing markdown-only insights, reusing existing script patterns where possible instead of inventing hidden one-off behavior. The script should upgrade legacy artifacts into the new JSON-first shape, log files that need manual review, and write a machine-readable migration status artifact (for example `.migration-status.json` under the configured insights base dir) that records remaining legacy artifact count and last-run time. The script must reuse the shared base-dir and safe `videoId` path helpers rather than rebuilding artifact paths ad hoc. Avoid setting up a permanent dual-format dependency in normal runtime code.</action>
  <verify>npx vitest run src/lib/__tests__/legacy-artifact-migration.test.ts</verify>
  <done>The repo has a clear one-time migration path, covered by tests or fixtures, and operators can tell when migration is complete instead of guessing.</done>
</task>

<task type="auto">
  <name>Task 2: Narrow runtime compatibility to migration-safe behavior</name>
  <files>src/lib/insights.ts, src/app/api/insight/route.ts, src/components/VideoAnalysisWorkspace.tsx</files>
  <action>Update runtime insight reading so the steady-state path is JSON-first and the remaining legacy handling is only what is necessary to survive the migration window or load already-upgraded artifacts cleanly. Avoid leaving broad permanent markdown-only branching in API/UI code. If any temporary fallback remains, make its intent narrow and explicit, and where useful consult the machine-readable migration status artifact from Task 1 rather than relying on operator memory alone.</action>
  <verify>npx vitest run src/lib/__tests__/analysis-contract.test.ts src/lib/__tests__/legacy-artifact-migration.test.ts && npm run e2e</verify>
  <done>The runtime clearly prefers JSON-backed artifacts, legacy handling is intentionally narrow, and the UI still works through the migration completion point.</done>
</task>

<task type="auto">
  <name>Task 3: Update operator and architecture docs for the new artifact model</name>
  <files>README.md, docs/architecture/artifact-schema.md, docs/architecture/analysis-runtime.md, docs/plans/2026-03-09-self-hosted-proxmox-deployment.md</files>
  <action>Document the new artifact model, the production base directory, and the one-time migration expectation. Ensure the docs consistently use `/srv/transcript-library/insights` as the canonical hosted path, explain that local development still defaults to repo `data/insights` unless configured otherwise, and document the operator check that confirms the migration window is finished. As part of this task, explicitly correct any stale markdown-first guidance in the self-hosted deployment plan.</action>
  <verify>npx eslint scripts/backfill-insight-artifacts.ts scripts/migrate-legacy-insights-to-json.ts src/lib/insights.ts src/app/api/insight/route.ts src/components/VideoAnalysisWorkspace.tsx src/lib/__tests__/legacy-artifact-migration.test.ts && npx prettier --check README.md docs/architecture/artifact-schema.md docs/architecture/analysis-runtime.md docs/plans/2026-03-09-self-hosted-proxmox-deployment.md && rg -n \"/srv/transcript-library/insights|INSIGHTS_BASE_DIR|analysis.json\" README.md docs src</verify>
  <done>The docs, deployment guidance, and runtime artifact model all describe the same JSON-first production story.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/lib/__tests__/legacy-artifact-migration.test.ts`
- [ ] `npm run e2e`
- [ ] `rg -n "/srv/transcript-library/insights|INSIGHTS_BASE_DIR|analysis.json" README.md docs src`
- [ ] `node scripts/migrate-legacy-insights-to-json.ts --check`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Legacy markdown-only insights have a one-time upgrade path rather than permanent long-term debt
- Phase 1 documentation and runtime behavior tell the same JSON-first deployment story
- Operators can verify when the migration window is complete
  </success_criteria>

<output>
After completion, create `.planning/phases/01-artifact-foundations/01-03-SUMMARY.md`
</output>
